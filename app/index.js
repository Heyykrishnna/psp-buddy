import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent calls AppRegistry.registerComponent('main', () => App)
// and ensures the app is correctly set up for Expo Go and native builds.
registerRootComponent(App);
